Para começar crie o ambiente virtual do python com o comando:

```
python3 -m venv venv
```

assim, entre no ambiente virtual

```
source ./venv/bin/active

``` 


assim, instale as dependencias usando o pip.

```
pip install -r requirements.txt
``` 